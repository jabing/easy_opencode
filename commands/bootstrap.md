# Bootstrap

Use `eoc bootstrap` to preview or apply ecosystem presets and bundle defaults for the current workspace.

Examples:

- `node scripts/bootstrap.js --json`
- `node scripts/bootstrap.js --apply --preset node-team --json`
- `node scripts/bootstrap.js --apply --bundle release-governance --json`
